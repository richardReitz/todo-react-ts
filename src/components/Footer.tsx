import React from 'react';
import styles from './Footer.module.css';

const Footer = () => {
    return(
        <header className={styles.footer}>
            <span>Developed by RR</span>
        </header>
    )
}

export default Footer;